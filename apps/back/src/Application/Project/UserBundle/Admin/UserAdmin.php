<?php
namespace App\Application\Project\UserBundle\Admin;

use App\Application\Project\ContentBundle\Service\AdminRoutesService;
use App\Application\Project\ContentBundle\Service\RolesIdentifierService;
use App\Application\Project\UserBundle\Entity\Group;
use App\Application\Project\UserBundle\Entity\User;
use ReflectionException;
use Sonata\AdminBundle\Admin\AbstractAdmin;
use Sonata\AdminBundle\Datagrid\DatagridMapper;
use Sonata\AdminBundle\Datagrid\ListMapper;
use Sonata\AdminBundle\Form\FormMapper;
use Sonata\AdminBundle\Form\Type\ModelType;
use Sonata\AdminBundle\Route\RouteCollectionInterface;
use Sonata\AdminBundle\Show\ShowMapper;
use Symfony\Bridge\Doctrine\Validator\Constraints\UniqueEntity;
use Symfony\Component\Form\Extension\Core\Type\ChoiceType;
use Symfony\Component\Form\Extension\Core\Type\PasswordType;
use Symfony\Component\Form\Extension\Core\Type\RepeatedType;
use Symfony\Component\Form\Extension\Core\Type\TextType;
use Symfony\Component\PasswordHasher\Hasher\UserPasswordHasherInterface;
use Symfony\Component\Validator\Constraints\Length;
use Symfony\Component\Validator\Constraints\NotBlank;


final class UserAdmin extends AbstractAdmin
{

    protected AdminRoutesService $routesService;
    protected UserPasswordHasherInterface $passwordHasher;

    private RolesIdentifierService $rolesIdentifierService;

    public function __construct(RolesIdentifierService $rolesIdentifierService, UserPasswordHasherInterface $passwordHasher)
    {
        parent::__construct();

        $this->rolesIdentifierService = $rolesIdentifierService;
        $this->passwordHasher = $passwordHasher;
    }

    public function toString(object $object): string
    {
        return $object instanceof User ? $object->getId()  . ' - ' . $object->getUsername() : '';
    }


    protected function generateBaseRoutePattern(bool $isChildAdmin = false): string
    {
        return 'user';
    }

   protected function configureRoutes(RouteCollectionInterface $collection): void
   {
       parent::configureRoutes($collection); // TODO: Change the autogenerated stub

   }

    /** @throws ReflectionException */
    protected function configureFormFields(FormMapper $form): void
    {
        $isEdit = $this->isCurrentRoute('edit') == 'edit';
        $passwordRequired = ! $isEdit;

        //dump($this->rolesIdentifierService->getAdminGroupRoles());

        $adminRoles = $this->rolesIdentifierService->getAdminRoles();
        $apiRoles = $this->rolesIdentifierService->getApiRoles();

        $choicesAdminRoles = $choicesApiRoles =[];

        //$choicesAdminRoles[] = [ "ROLE_SUPER_ADMIN" => "ROLE_SUPER_ADMIN" ];
        foreach ($adminRoles as $role) {
            $choicesAdminRoles[] = [ $role['role'] => $role['role'] ];
        }

        //$choicesAdminRoles[] = [ "ROLE_SUPER_API" => "ROLE_SUPER_API" ];
        foreach ($apiRoles as $role) {
            $choicesApiRoles[] = [ $role['role'] => $role['role'] ];
        }

        $form->tab('Geral');

            $form->with('Informações Gerais', ['class' => 'col-md-8']);

                $form->add('username', TextType::class, [
                    'label' => 'Nome do Usuário:',
                    'required' => true,
                ]);

                $form->add('email', TextType::class, [
                    'label' => 'E-mail:',
                    'required' => true,
                ]);


                $form->add('password', RepeatedType::class, array(
                    'type' => PasswordType::class,
                    'first_options' => array(
                        'label' => 'Senha',
                    ),
                    'second_options' => array(
                        'label' => 'Confirmar Senha',
                    ),
                    'required' => true,
                ));

                $passwordOptions = array(
                    'type' => PasswordType::class,
                    'required' => $passwordRequired,
                    'invalid_message' => 'Os campos de senha devem ser iguais.',
                    'first_options' => array(
                        'label' => 'Senha'
                    ),
                    'second_options' => array(
                        'label' => 'Confirmar senha'
                    )
                );

                if ($passwordRequired) {
                    $passwordOptions['constraints'] = array(
                        new NotBlank(array(
                            'message' => 'É necessário informar uma senha.'
                        )),
                        new Length(array(
                            'min' => 5,
                            'minMessage' => 'A senha deve ter mais de 5 caracteres.'
                        ))
                    );
                }

                $form->add('password', RepeatedType::class, $passwordOptions);



            $form->end();

            $form->with('Grupos do Usuário', ['class' => 'col-md-4']);

                $form->add('groups', ModelType::class,[
                    'class' => Group::class,
                    'property' => 'name',
                    'label' => 'Grupos',
                    'required' => false,
                    'expanded' => true,
                    'btn_add' => false,
                    'multiple' => true,
                ]);

            $form->end();

        $form->end();

        $form->tab('Admin');
            $form->with('Permissões Administrativas');

            $form->add('adminRoles', ChoiceType::class, [
                'label' => ' ',
                'required' => false,
                'multiple' => true,
                'expanded'=> false,
                'choices' => $choicesAdminRoles,
                'attr' => [
                    'class' => 'div-select-admin-roles',
                    //'style' => 'display:none;'
                ],
            ]);

            $form->end();
        $form->end();

        $form->tab('Api');
            $form->with('Permissões Api');

                $form->add('apiRoles', ChoiceType::class, [
                    'label' => ' ',
                    'required' => false,
                    'multiple' => true,
                    'expanded'=> false,
                    'choices' => $choicesApiRoles,
                    'attr' => [
                        'class' => 'div-select-api-roles',
                        //'style' => 'display:none;'
                    ],
                ]);

            $form->end();
        $form->end();


    }

    protected function configureDatagridFilters(DatagridMapper $datagrid): void
    {
        $datagrid->add('username');
        $datagrid->add('email');
        $datagrid->add('roles');
    }

    protected function configureListFields(ListMapper $list): void
    {
        $list->addIdentifier('username');
        $list->addIdentifier('email');
        //$list->addIdentifier('roles');
        $list->add('groups', null, [
            'label' => 'Grupos',
            'associated_property' => 'name',
        ]);
        $list->add(ListMapper::NAME_ACTIONS, ListMapper::TYPE_ACTIONS, [
            'actions' => [
                'show' => [],
                'edit' => [],
                'delete' => [],
            ]
        ]);
    }

    protected function configureShowFields(ShowMapper $show): void
    {
        $show->add('username');
        $show->add('email');
        $show->add('groups', null, [
            'associated_property' => 'name',
            'label' => 'Grupo:',
        ]);
        $show->add('roles');
    }

    protected function preUpdate(object $object): void
    {
        //$this->getModelManager()->upgradePassword($object, $object->getPassword());

       /* if( !$this->passwordHasher->isPasswordValid( $object, $object->getPassword() ) ){
            $object->setPassword( $this->passwordHasher->hashPassword( $object, $object->getPassword() ) );
        }*/


        parent::preUpdate($object); // TODO: Change the autogenerated stub

    }
}

























