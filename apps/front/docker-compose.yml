version: "3.7"

services:
    web:
      image: nginx
      volumes:
        - ./docker/nginx/nginx.conf:/etc/nginx/nginx.conf
        - ./dist:/usr/share/nginx/html
      ports:
        #- "80:80"
        - "9000:80"
      restart: always
